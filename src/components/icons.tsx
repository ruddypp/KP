import { LucideProps, Loader2 } from "lucide-react";

export const Icons = {
  spinner: (props: LucideProps) => <Loader2 {...props} />,
  // Tambahkan ikon lain di sini jika perlu
  // contoh:
  // logo: (props: LucideProps) => <Command {...props} />,
}; 